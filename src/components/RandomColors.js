import React from "react";

const RandomColors = (props) => {
  return (
    <>
      <button className="btn btn-light" onClick={props.handlechange3}>
        Random Generator
      </button>
    </>
  );
};

export default RandomColors;
